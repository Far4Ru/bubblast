#ifndef EXIT_BUTTON_H
#define EXIT_BUTTON_H

#include "engine/engine.hpp"

class ExitButton {
    public:
        ExitButton();
        ~ExitButton();
    private:
        TextObject* text;
};

#endif
