#include "engine/renderer.hpp"

int Renderer::create() {
    // this->core = SDL_CreateRenderer(engine.window->get(), -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC);
    // if (this->core == NULL){
    //     std::cout << SDL_GetError() << std::endl;
    //     return 1;
    // }
    return 0;
}